# 论点修订 eval

> 验收标准：一次修订好不好，看这 5 条。最关键是第 2 条——**留痕是否让日后能说清当初为什么改**。

## 1. 最小修订面

是否只改了被证据推翻的那几格，而不是顺手重写整个论点？

- ✅ 只改 solutionHypothesis，其他格不动。
- ❌ 每格都重写一遍，分不清到底什么被证伪了。

## 2. 留痕完整（最关键）

`revisions[]` 是否有 `at` + `reason`，且 reason 点名了触发修订的假设 ID？

- ✅ “B-02 refuted：目标用户不为自动化付费，改为…”，before/after 都在。
- ❌ 默默改了 statement，没留 revisions。

## 3. 复位正确

改完是否把 `needsRevision` 置回 false，让回路可以重新跑起？

- ✅ needsRevision: true → false，validate 不再报 ⚠️。
- ❌ 改了论点却忘了复位，台账永远提醒待修订。

## 4. 触发新红队

收尾是否指向 `@assumption-xray`（新论点会带出新裸奔假设），而不是直接去造？

- ✅ “新论点待红队”。
- ❌ “论点改好了，开始做吧”。

## 5. 诚实

pivot 是否被诚实地标为 pivot，而不是伪装成一次小修？该 kill 的时候是否敢于承认而硬修？

- ✅ 每格都在改时，直言“这不是修订，是重新想”。
- ❌ 把推倒重来包装成“微调”。
